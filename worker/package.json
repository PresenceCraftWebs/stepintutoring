{
  "name": "sit-admin-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Step-In Tutoring admin Worker: signed R2 uploads, video deletion, pipeline triggers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260708.1",
    "typescript": "^5.8.0",
    "wrangler": "^4.0.0"
  }
}
