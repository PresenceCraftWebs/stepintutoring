# Local development env (loaded in `npm run dev`; .env.production overrides
# these for production builds). Safe to commit — these are public URLs, not
# secrets.

# Admin Worker — set here too so the admin gate + tools work in local dev.
VITE_WORKER_URL=https://sit-admin.nduvho-ramashia.workers.dev

# Video base URL for real (non-seed) lessons.
VITE_R2_PUBLIC_URL=https://pub-9527cf52a5934a63a611cee174b80a08.r2.dev

# Deliberately EMPTY in dev: local content/ edits should show immediately
# instead of being shadowed by the deployed curriculum.json.
VITE_CONTENT_BASE_URL=
